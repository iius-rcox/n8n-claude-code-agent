apiVersion: v1
kind: Service
metadata:
  name: ops-dashboard
  namespace: ops-dashboard
  labels:
    app: ops-dashboard
spec:
  type: ClusterIP
  selector:
    app: ops-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
